# Dependências de build/desenvolvimento (NÃO são dependências de runtime).
# Instale junto com requirements.txt apenas para gerar o executável ou rodar o lint.
pyinstaller==6.21.0
pyflakes==3.4.0

# Testes — instale com: pip install -r requirements-dev.txt ; depois rode `pytest`.
pytest==8.3.4
pytest-mock==3.14.0
pytest-cov==6.0.0
